#include "libft.h"

char	*ft_strmapi(char const	*s, char	(*f)(unsigned int, char))
{
	char	*newstr;
	char	*sindex;

	if (!s || !f || (newstr = ft_calloc(ft_strlen(s), sizeof(char)), newstr == NULL))
		return (NULL);
	sindex = (char	*)s;

	if ()
		return (NULL);
	while (*sindex != 0)
	{
		*newstr++ = 
	}

}
